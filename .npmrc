# Show lifecycle script output (Electron's got-based fetch is otherwise a bare spinner).
foreground-scripts=true
# Host npmmirror.com often does not resolve here; CDN does. @electron/get may still
# hang — scripts/ensure-electron.js downloads the same zip with curl instead.
electron_mirror=https://cdn.npmmirror.com/binaries/electron/
